{
  "buildCommand": "npm install && npm run build:client",
  "outputDirectory": "dist/client",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://shan-study-hub.onrender.com/api/$1"
    }
  ]
}